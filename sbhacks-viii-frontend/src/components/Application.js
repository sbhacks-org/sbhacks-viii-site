import React, { useEffect, useState } from "react";
import {
    getAuth,
    onAuthStateChanged,
    signOut,
} from "firebase/auth";

const Application = () => {
    return (
        <div>
            Application Page
        </div>
    )
}

export default Application;